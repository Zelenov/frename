[package]
name = "frename-core"
version = "0.1.0"
edition = "2021"
description = "Core logic for frename file renaming utility"

[dependencies]
log = "0.4"
tokio = { version = "1", features = ["fs", "time"] }
